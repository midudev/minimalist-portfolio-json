---
import Layout from "@/layouts/Layout.astro"
import AllSections from "@/components/AllSections.astro"

import cv from "@cv-en"
const { name, label } = cv.basics
---

<Layout title={`${name} Portfolio - ${label}`} cv={cv}>
  <AllSections cv={cv} />
</Layout>
